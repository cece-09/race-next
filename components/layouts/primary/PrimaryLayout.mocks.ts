import { IPrimaryLayout } from "./PrimaryLayout";

const loggedIn: IPrimaryLayout = {
  sitename: "race review",
};

const loggedOut: IPrimaryLayout = {
  sitename: "race review",
};

export const mockPrimaryLayout = {
  loggedIn,
  loggedOut,
};
